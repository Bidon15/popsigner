{
  "name": "nitro-deployer-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Nitro chain deployment worker with POPSigner mTLS integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "nitro-deployer": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "deploy": "ts-node src/cli.ts",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@arbitrum/orbit-sdk": "^0.24.2",
    "viem": "^1.21.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "patch-package": "^8.0.1",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
