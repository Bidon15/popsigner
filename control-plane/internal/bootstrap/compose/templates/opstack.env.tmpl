# =============================================================
# {{ .ChainName }} - OP Stack Configuration
# Chain ID: {{ .ChainID }}
# =============================================================
# Generated by POPSigner - https://popsigner.com

# L1 Configuration
# Replace with your L1 RPC endpoint (Alchemy, Infura, self-hosted, etc.)
L1_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_KEY

# POPSigner Configuration (API Key Auth)
# Get your API key from the POPSigner dashboard
POPSIGNER_RPC_URL={{ if .POPSignerRPCEndpoint }}{{ .POPSignerRPCEndpoint }}{{ else }}https://rpc.popsigner.com{{ end }}
POPSIGNER_API_KEY=bbr_live_xxxxxxxxxxxxxxxxxxxxx

# Role Addresses (from deployment)
# These are the addresses POPSigner manages for signing
BATCHER_ADDRESS={{ .BatcherAddress }}
PROPOSER_ADDRESS={{ .ProposerAddress }}
{{ if .GetL2OutputOracle -}}
L2_OUTPUT_ORACLE_ADDRESS={{ .GetL2OutputOracle }}
{{- else -}}
L2_OUTPUT_ORACLE_ADDRESS=0x0000000000000000000000000000000000000000
{{- end }}
{{ if .UseCelestia }}
# =============================================================
# Celestia DA Configuration
# =============================================================
# Celestia keyring is managed by POPSigner - you just need the node connection
CELESTIA_RPC_URL={{ if .CelestiaRPC }}{{ .CelestiaRPC }}{{ else }}https://celestia-rpc.popsigner.com{{ end }}
CELESTIA_AUTH_TOKEN=your_auth_token_here
CELESTIA_NAMESPACE=your_namespace_hex
{{ end }}
# =============================================================
# GETTING STARTED
# =============================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace placeholder values with your actual credentials
# 3. Run: docker compose up -d
# 4. Check health: ./scripts/healthcheck.sh
#
# Documentation: https://docs.popsigner.com/opstack

