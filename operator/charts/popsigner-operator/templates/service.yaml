{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "popsigner-operator.fullname" . }}-metrics
  labels:
    {{- include "popsigner-operator.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "popsigner-operator.selectorLabels" . | nindent 4 }}
{{- end }}
