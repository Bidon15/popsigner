{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "popsigner-operator.fullname" . }}-manager
  labels:
    {{- include "popsigner-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "popsigner-operator.fullname" . }}-manager
subjects:
  - kind: ServiceAccount
    name: {{ include "popsigner-operator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
