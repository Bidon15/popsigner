{{- if .Values.installCRDs }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: banhbaoringbackups.banhbaoring.io
  labels:
    {{- include "banhbaoring-operator.labels" . | nindent 4 }}
spec:
  group: banhbaoring.io
  names:
    kind: BanhBaoRingBackup
    listKind: BanhBaoRingBackupList
    plural: banhbaoringbackups
    singular: banhbaoringbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.totalSize
      name: Size
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: BanhBaoRingBackup is the Schema for the banhbaoringbackups API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: BanhBaoRingBackupSpec defines the desired state
            properties:
              clusterRef:
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              components:
                default:
                - openbao
                - database
                - secrets
                description: Components to backup
                items:
                  type: string
                type: array
              destination:
                description: Override cluster backup destination
                properties:
                  gcs:
                    properties:
                      bucket:
                        type: string
                      credentials:
                        properties:
                          key:
                            type: string
                          name:
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      prefix:
                        type: string
                    required:
                    - bucket
                    - credentials
                    type: object
                  s3:
                    properties:
                      bucket:
                        type: string
                      credentials:
                        properties:
                          key:
                            type: string
                          name:
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      prefix:
                        type: string
                      region:
                        type: string
                    required:
                    - bucket
                    - credentials
                    type: object
                type: object
              type:
                default: full
                enum:
                - full
                - incremental
                type: string
            required:
            - clusterRef
            type: object
          status:
            description: BanhBaoRingBackupStatus defines the observed state
            properties:
              completedAt:
                format: date-time
                type: string
              components:
                items:
                  properties:
                    location:
                      type: string
                    name:
                      type: string
                    size:
                      type: string
                    status:
                      type: string
                  required:
                  - name
                  - status
                  type: object
                type: array
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Pending, Running, Completed, Failed
                type: string
              startedAt:
                format: date-time
                type: string
              totalSize:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
