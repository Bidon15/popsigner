{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "banhbaoring-operator.fullname" . }}-metrics
  labels:
    {{- include "banhbaoring-operator.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "banhbaoring-operator.selectorLabels" . | nindent 4 }}
{{- end }}
